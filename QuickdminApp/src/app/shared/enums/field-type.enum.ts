export enum FieldTypeEnum {
  text = 'text',
  email = 'email',
  number = 'number',
  select = 'select',
  upload = 'upload',
  password = 'password',
  checkbox = 'checkbox',
  textarea = 'textarea',
}