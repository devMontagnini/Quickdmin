import { InjectionToken } from "@angular/core";

export const MODEL_SERVICE_TOKEN = new InjectionToken('model.service');
export const AUTHENTICATION_SERVICE_TOKEN = new InjectionToken('authentication.service');
export const AUTHENTICATED_USER_SERVICE_TOKEN = new InjectionToken('authenticated-user.service');
